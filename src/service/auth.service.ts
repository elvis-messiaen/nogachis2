import { rejects } from "assert";
import { resolve } from "dns";

export class AuthService [
    isAuth = false;

    signIn () {
        return new Promise (
            executor; (resolve,rejects) => {
                setTimeout (
                    handler: () => {
                        this.isAuth = true;
                        resolve(true);
                    }, 2000
                );
            }
        );
    }
    

]